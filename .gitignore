node_modules/
dist/
*.log
*.tgz
.env
.env.*

mcp-publisher.exe
mcp-publisher.tar.gz

# MCPB bundle: a build artifact (3MB, vendors node_modules). Rebuild with:
#   mcpb pack <staging-dir> prometiam-risk-mcp.mcpb
*.mcpb
